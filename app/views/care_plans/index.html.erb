<p id="notice"><%= notice %></p>

<h1>Care Plans</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @care_plans.each do |carePlan| %>
      <tr>
        <td><%= link_to carePlan.id, care_plan_path(carePlan.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Care Plan', new_care_plan_path %>
