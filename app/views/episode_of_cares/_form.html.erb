<%= form_with(model: episode_of_care, local: true) do |form| %>
  <% if episode_of_care.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(episode_of_care.errors.count, "error") %> prohibited this episode_of_care from being saved:</h2>

      <ul>
      <% episode_of_care.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
