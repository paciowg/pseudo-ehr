<!-- app/views/transition_of_cares/index.html.erb -->
<div class="container mx-auto lg:ml-64 px-4 py-8 max-w-7xl lg:w-5/6">
  <div class="mb-8 flex flex-col md:flex-row md:items-center md:justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900 mb-2">Transition of Care</h1>
      <p class="text-gray-600">Managing patient care transitions</p>
    </div>
    <div class="mt-4 md:mt-0">
      <button type="button" data-modal-target="createTocResourceModal" data-modal-toggle="createTocResourceModal"
        class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
        <svg xmlns="http://www.w3.org/2000/svg" class="-ml-1 mr-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
        </svg>
        Create New TOC
      </button>
    </div>
  </div>

  <%= render "transition_of_cares" %>
  <%= render "create_toc_modal" %>
  <%= render "edit_toc_modal" %>
  <%= render "notify_toc_modal" %>
</div>
