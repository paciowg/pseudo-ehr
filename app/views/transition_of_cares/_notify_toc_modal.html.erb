<!-- Modal for sharing a TOC -->
<% @tocs.each do |toc| %>
  <% identifier = toc.id.to_s.downcase %>
  <div id="share-<%= identifier %>" tabindex="-1" aria-hidden="true" class="fixed top-0 left-0 right-0 z-50 hidden w-full p-4 overflow-x-hidden overflow-y-auto md:inset-0 h-[calc(100%-1rem)] max-h-full">
    <div class="relative w-full max-w-2xl max-h-full">
      <div class="relative bg-white rounded-lg shadow dark:bg-gray-700">
        <div class="flex items-center justify-between p-4 md:p-5 border-b rounded-t dark:border-gray-600 bg-gradient-to-r from-indigo-600 to-indigo-800">
          <h3 class="text-xl font-semibold text-white">
            Share Transition of Care Data
          </h3>
          <button type="button" class="text-white bg-transparent hover:bg-indigo-700 hover:text-white rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center" data-modal-hide="share-<%= identifier %>">
            <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
            </svg>
            <span class="sr-only">Close modal</span>
          </button>
        </div>

        <div class="p-4 md:p-5 space-y-4">
          <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-blue-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm text-blue-700">
                  <strong>Document:</strong> <%= toc.title %><br>
                  <strong>Date:</strong> <%= toc.date %>
                </p>
              </div>
            </div>
          </div>

          <%= form_with url: share_patient_transition_of_care_path(patient_id: @patient.id, id: toc.id), method: :post, id: "share-toc-form-#{identifier}", class: "space-y-6" do |form| %>
            <div>
              <label class="block mb-2 text-sm font-medium text-gray-900">
                Share Mode <span class="text-red-500">*</span>
              </label>
              <div class="space-y-2">
                <label class="flex items-center">
                  <%= form.radio_button :share_mode, 'document', checked: true, class: "w-4 h-4 text-indigo-600 bg-gray-100 border-gray-300 focus:ring-indigo-500" %>
                  <span class="ml-2 text-sm text-gray-900">Share Document</span>
                </label>
                <label class="flex items-center">
                  <%= form.radio_button :share_mode, 'message', class: "w-4 h-4 text-indigo-600 bg-gray-100 border-gray-300 focus:ring-indigo-500" %>
                  <span class="ml-2 text-sm text-gray-900">Send Message</span>
                </label>
              </div>
              <p class="mt-2 text-sm text-gray-500">
                Share Document creates a TOC bundle and DocumentReference on the selected FHIR server. Send Message creates a TOC bundle and discharge notification message on the selected FHIR server.
              </p>
            </div>

            <div>
              <label for="target_fhir_server_id_<%= identifier %>" class="block mb-2 text-sm font-medium text-gray-900">
                FHIR Server <span class="text-red-500">*</span>
              </label>
              <%= form.select :target_fhir_server_id,
                  options_for_select(FhirServer.order(:name).map { |server| ["#{server.name} (#{server.base_url})", server.id] }),
                  { include_blank: "Select FHIR Server" },
                  { class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-indigo-500 focus:border-indigo-500 block w-full p-2.5", required: true, id: "target_fhir_server_id_#{identifier}" }
              %>
            </div>

            <div>
              <label for="destination_organization_<%= identifier %>" class="block mb-2 text-sm font-medium text-gray-900">
                Destination Organization <span class="text-red-500">*</span>
              </label>
              <%= form.select "destination_organization",
                  options_for_select(cached_resources_for_select('Organization').map do |org|
                    ["#{org.name || 'Organization'}", "Organization/#{org.id}"]
                  end),
                  { include_blank: "Select Destination Organization" },
                  { class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-indigo-500 focus:border-indigo-500 block w-full p-2.5", required: true, id: "destination_organization_#{identifier}" }
              %>
              <p class="mt-2 text-sm text-gray-500">
                Select the organization that should receive this transition of care data.
              </p>
            </div>

            <div class="flex justify-end border-t border-gray-200 pt-4">
              <button type="button" id="cancel-share-btn-<%= identifier %>" data-modal-hide="share-<%= identifier %>" class="text-gray-500 bg-white hover:bg-gray-100 focus:ring-4 focus:outline-none focus:ring-indigo-300 rounded-lg border border-gray-200 text-sm font-medium px-5 py-2.5 hover:text-gray-900 focus:z-10 mr-2">
                Cancel
              </button>
              <button type="submit" id="submit-share-btn-<%= identifier %>" class="text-white bg-indigo-600 hover:bg-indigo-700 focus:ring-4 focus:outline-none focus:ring-indigo-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center inline-flex items-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="-ml-1 mr-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h8m-8 4h8m-8-8h8M5 6h14a2 2 0 012 2v8a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2z" />
                </svg>
                Share Transition of Care Data
              </button>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>
