    <table class='table table-striped table-dark status'>
      <tr>
        <td>
          Patient: <%= link_to display_human_name(@patient.names.first), dashboard_path(patient: @patient.id) %>
        </td>
        <td>Age: <%= sanitize(@patient.age) %></td>
        <td>DOB: <%= display_date(@patient.birth_date) %></td>
        <td>Gender: <%= sanitize(@patient.gender) %></td>
      </tr>
    </table>